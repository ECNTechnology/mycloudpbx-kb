backend:
  name: git-gateway
  # repo: "ECNTechnology/knowledge-base"
  branch: Production # Branch to update (optional; defaults to master)
publish_mode: editorial_workflow
media_folder: "/.vuepress/public/images"
public_folder: "/images"
collections:
  - name: "mycloudpbx" # Used in routes, e.g., /admin/collections/blog
    label: "myCloudPBX articles" # Used in the UI
    folder: "/guides/mycloudpbx" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - {label: "Title", name: "title", widget: "string"}
      - { label: 'Publish Date', name: 'date', widget: 'datetime' }
      - {label: "Body", name: "body", widget: "markdown"}
  - name: "business-sip" # Used in routes, e.g., /admin/collections/blog
    label: "Business SIP articles" # Used in the UI
    folder: "/guides/business-sip" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - {label: "Title", name: "title", widget: "string"}
      - { label: 'Publish Date', name: 'date', widget: 'datetime' }
      - {label: "Body", name: "body", widget: "markdown"}
  - name: "email-services" # Used in routes, e.g., /admin/collections/blog
    label: "Email Services articles" # Used in the UI
    folder: "/guides/email-services" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - {label: "Title", name: "title", widget: "string"}
      - { label: 'Publish Date', name: 'date', widget: 'datetime' }
      - {label: "Body", name: "body", widget: "markdown"}
  - name: "internet-services" # Used in routes, e.g., /admin/collections/blog
    label: "Internet Services articles" # Used in the UI
    folder: "/guides/internet-services" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - {label: "Title", name: "title", widget: "string"}
      - { label: 'Publish Date', name: 'date', widget: 'datetime' }
      - {label: "Body", name: "body", widget: "markdown"}
  - name: "inbound-1300" # Used in routes, e.g., /admin/collections/blog
    label: "Inbound 1300 articles" # Used in the UI
    folder: "/guides/inbound-1300" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - {label: "Title", name: "title", widget: "string"}
      - { label: 'Publish Date', name: 'date', widget: 'datetime' }
      - {label: "Body", name: "body", widget: "markdown"}
  - name: "cloud-dial" # Used in routes, e.g., /admin/collections/blog
    label: "CloudDial articles" # Used in the UI
    folder: "/guides/cloud-dial" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - {label: "Title", name: "title", widget: "string"}
      - { label: 'Publish Date', name: 'date', widget: 'datetime' }
      - {label: "Body", name: "body", widget: "markdown"}